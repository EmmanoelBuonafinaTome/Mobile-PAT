import { StyleSheet } from 'react-native';
import { COLORS } from '../../theme';

const styles = StyleSheet.create({
  input: {
    borderWidth: 1,
    borderRadius: 10,
    height: 30,
    width: '80%',
    marginTop: 10
  }
})

export { styles }